<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.healthtrack">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HealthTrack">
        <activity android:name=".CovidandVaccinesActivity"></activity>
        <activity android:name=".WebMapActivity" />
        <activity android:name=".NewProfileActivity" />
        <activity android:name=".NewAppointmentActivity" />
        <activity android:name=".CaregiverMenuActivity"></activity>
        <activity android:name=".CaregiverSignupActivity" />
        <activity android:name=".EditAppointmentActivity" />
        <activity android:name=".EditPractitionerActivity" />
        <activity android:name=".NewPractitionerActivity" />
        <activity android:name=".NewProfileActivity" />
        <activity android:name=".PatientSignUpActivity" />
        <activity android:name=".AccounTypeActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".NewAppointmentActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".CovidActivity" />
        <activity android:name=".PractitionersActivity" />
        <activity android:name=".AppointmentsActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>